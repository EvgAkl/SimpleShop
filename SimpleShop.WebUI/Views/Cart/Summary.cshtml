@model SimpleShop.Domain.Entities.Cart

<div class="navbar-text">
    <b>Ваша козина:</b>
    @Model.Lines.Sum(x => x.Quantity) игр
    @Model.ComputeTotalValue().ToString("# руб")
</div>

@Html.ActionLink("Заказать", "Index", "Cart", new { retunUrl = Request.Url.PathAndQuery }, new { @class = "btn btn-default navbar-btn " })


